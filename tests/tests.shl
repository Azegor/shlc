// >>>>>>> stdlib declarations:

fn prt(int i) native "prt_i";
fn prt_w(int i, w) native "prt_w_i";
fn prtln(int i) native "prtln_i";

fn prt(flt f) native "prt_f";
fn prt_w(flt f, w) native "prt_w_f";
fn prtln(flt f) native "prtln_f";

fn prt(chr c) native "prt_c";
fn prtln(chr c) native "prtln_c";

fn prt(boo b) native "prt_b";
fn prtln(boo b) native "prtln_b";

fn prt(str s) native "prt_s";
fn prtln(str s) native "prtln_s";

// <<<<<<<

fn dce()
{
    if F {
      prtln(2147483647);
    }
    whl F {
      prtln(0-2147483648);
    }

    var a = F;
    var b = T;

    if a && b && 1{
      prtln(51717);
    }
}

fn missingReturn(boo a, b, c, d) : int
{
  if a {
    if b {
      if c {
        if d {ret 1;}
      } el {ret 1;}
    }
  } elif (b) {
    ret 1;
  } el {
    whl a && b { ret 1;}
  }
  ret 1;
}

fn flt_neg()
{
  var i = 1;
  var f = 1.0;

  var ni = -i;
  var nf = -f;
}

fn useVar(boo b){}
fn useVar(int i){}

fn expressions()
{
  var x : int;
  var y = 5;
  var b1, b2 : boo;

  /* type of system.out.println arg - is int */

  prtln(x);
  prtln(y);

  /* unary expressions */

  y = -x; x = -1;
  b1 = !T; b2 = !b1;

  /* *,/,% */

  x = 5 * 15352;
  y = x * x;
  y = 2345 / y;
  y = x % (-y);
  x = -5 / -6;

  /* +,- */

  y = x + x; y = x - x;
//   y = (x * x) + (y / y) - (x % y); // makes it undefined!

  /* <. <=, >, >= */

  b1 = (x < y) : boo; b1 = (y <= x) : boo; b1 = (x > x) : boo; b2 = (x >= 5) : boo;

  b1 = (x*x - y % y <= 522) : boo;
  b1 = (x*x -y+y >= 5 * 6 - 7 + 123 + x) : boo;

  /* ==, != */

  b1 = b1 == b2;
  b1 = x == y;
  b2 = b1 != b2;
  b2 = x != y;
  b1 = 25 == 52;
  b1 = 25 != 52;

  /* &&, || */

  b1 = T && F;
  b1 = T || F;
  b1 = b1 && b2;
  b1 = !b1 && !b2;
  b1 = b1 || b2;
  b1 = !b1 || !b2;
  b1 = (x < y) || (x > y) && (x * x <= x / x) || (((((F)))));

  useVar(x);
  useVar(y);
  useVar(b1);
  useVar(b2);
}

fn main()
{
  dce();
  missingReturn(T, F, T, F);
  flt_neg();
  expressions();
}